[project]
name = "orderbook"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "sortedcontainers>=2.4.0",
]

[tool.uv]
package = false
